// Re-export for backward compatibility during migration
export type { Room, Guest, Charge, RoomStatus } from './types';
export { rooms, guests, starterCharges, statusLabels } from './fixtures';
export { money } from './format';
