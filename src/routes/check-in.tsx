import { useState } from 'react';
import { CalendarDays, Check, ChevronDown, FileText, Search, Trash2, Upload, UserPlus } from 'lucide-react';
import { guests, money, type Guest } from '@/data/hotel';
import { PageHeader, Toast } from '@/routes/__root';

export function CheckInPage() {
  const [selected, setSelected] = useState<Guest>(guests[0]); const [name, setName] = useState(guests[0].name); const [adults, setAdults] = useState(guests[0].adults); const [kids, setKids] = useState(guests[0].kids); const [rate, setRate] = useState(guests[0].rate); const [extra, setExtra] = useState(200); const [toast, setToast] = useState('');
  const gst = Math.round(rate * 0.1); const total = rate + gst + extra; const showToast = (message: string) => { setToast(message); window.setTimeout(() => setToast(''), 2300); };
  const chooseGuest = (guest: Guest) => { setSelected(guest); setName(guest.name); setAdults(guest.adults); setKids(guest.kids); setRate(guest.rate); };
  return <div className="page-wrap"><PageHeader title="Guest check-in" subtitle="Prepare arrivals, verify details, and collect payment before handing over the room."><button className="button secondary"><UserPlus size={16} /> Add guest</button></PageHeader><div className="checkin-layout"><div className="checkin-main"><section className="split-panels"><div className="panel"><PanelTitle number="01" title="Select booking & guest" /><div className="panel-body"><label className="field full"><span>Search booking ID / guest name</span><div className="input-icon"><Search size={15} /><input placeholder="Search by booking or guest" /></div></label><label className="field full"><span>Select customer</span><select value={selected.name} onChange={(event) => chooseGuest(guests.find((guest) => guest.name === event.target.value) ?? guests[0])}>{guests.map((guest) => <option key={guest.name}>{guest.name}</option>)}</select></label><div className="booking-meta"><span>Booking date<strong>02 Apr 2026</strong></span><span>Booking time<strong>07:00 PM <CalendarDays size={14} /></strong></span></div></div></div><div className="panel"><PanelTitle number="02" title="Review & update details" /><div className="panel-body form-grid"><div className="room-badge"><span>Room no.</span><b><span>▰</span> {selected.room}</b></div><Field label="Rent"><input type="number" value={rate} onChange={(event) => setRate(Number(event.target.value))} /></Field><Field label="GST"><input value={gst} readOnly /></Field><Field label="Guest name"><input value={name} onChange={(event) => setName(event.target.value)} /></Field><Field label="Adults"><input type="number" value={adults} onChange={(event) => setAdults(Number(event.target.value))} /></Field><Field label="Kids"><input type="number" value={kids} onChange={(event) => setKids(Number(event.target.value))} /></Field><Field label="Checkout date"><input type="date" defaultValue={selected.checkout} /></Field><div className="upload-box"><Upload size={16} /><span>Upload ID proof</span><small>{selected.idProof}</small></div><Field label="Additional charges"><input type="number" value={extra} onChange={(event) => setExtra(Number(event.target.value))} /></Field><div className="detail-actions"><button className="button ghost"><Trash2 size={15} /> Delete</button><button className="button secondary">Edit details</button><button className="button primary" onClick={() => showToast('Guest details updated')}><Check size={15} /> Update</button></div></div></div></section><section className="panel table-panel"><div className="section-heading"><div><span className="eyebrow">Arrival register</span><h2>Current check-ins</h2></div><span className="count-pill">{guests.length} arrivals</span></div><div className="table-scroll"><table><thead><tr><th>Room</th><th>Rent</th><th>GST</th><th>Guest name</th><th>Adults</th><th>Kids</th><th>Checkout</th><th>ID proof</th><th /></tr></thead><tbody>{guests.map((guest) => <tr key={guest.room}><td><b>{guest.room}</b></td><td>{money(guest.rate)}</td><td>{money(guest.gst)}</td><td><b>{guest.name}</b></td><td>{guest.adults}</td><td>{guest.kids}</td><td>{guest.checkout}</td><td><span className="file-name"><FileText size={14} />{guest.idProof}</span></td><td><button className="table-action" onClick={() => chooseGuest(guest)}>Edit</button></td></tr>)}</tbody></table></div></section></div><aside className="panel payment-panel"><PanelTitle number="03" title="Finalize check-in & payment" /><div className="payment-content"><div className="summary-list"><span>Room charge <b>{money(rate)}</b></span><span>Additional charges <b>{money(extra)}</b></span><span>Tax <b>{money(gst)}</b></span></div><div className="total-line"><span>Total amount</span><strong>{money(total)}</strong></div><label className="field full"><span>Amount paid</span><input type="number" defaultValue={total} /></label><button className="button primary full-button" onClick={() => showToast(`Check-in confirmed for ${name}`)}><Check size={16} /> Complete check-in</button><div className="button-row"><button className="button secondary">Get data</button><button className="button secondary">Print</button></div><button className="button ghost full-button">Print registration card</button><button className="button ghost full-button">Download folio</button></div></aside></div>{toast && <Toast message={toast} />}</div>;
}
function PanelTitle({ number, title }: { number: string; title: string }) { return <div className="panel-title"><span>{number}</span><h2>{title}</h2><ChevronDown size={17} /></div>; }
function Field({ label, children }: { label: string; children: React.ReactNode }) { return <label className="field"><span>{label}</span>{children}</label>; }
